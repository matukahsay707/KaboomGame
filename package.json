{
  "name": "kaboom-game",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "build:shared": "npm run build --workspace=@kaboom/shared",
    "build:server": "npm run build --workspace=@kaboom/server",
    "build:client": "npm run build --workspace=@kaboom/client",
    "build": "npm run build --workspace=@kaboom/shared && npm run build --workspace=@kaboom/server && npm run build --workspace=@kaboom/client",
    "start": "node server/dist/index.js",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.4.5"
  }
}
